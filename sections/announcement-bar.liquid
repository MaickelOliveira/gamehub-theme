{% if section.settings.message != blank %}
  <div class="announcement-bar">
    <div class="announcement-bar__track">
      <div class="announcement-bar__group">
        {%- for i in (1..8) -%}
          <p{% unless forloop.first %} aria-hidden="true"{% endunless %}>{{ section.settings.message }} &middot;</p>
        {%- endfor -%}
      </div>
      <div class="announcement-bar__group" aria-hidden="true">
        {%- for i in (1..8) -%}
          <p aria-hidden="true">{{ section.settings.message }} &middot;</p>
        {%- endfor -%}
      </div>
    </div>
  </div>
{% endif %}

{% schema %}
{
  "name": "Barra de anúncio",
  "settings": [
    {
      "type": "text",
      "id": "message",
      "label": "Mensagem",
      "default": "Frete grátis acima de R$ 150"
    }
  ]
}
{% endschema %}
